create table User{
 use_type number;
 use_name varchar(20);
 use_email varchar(30);
 use_phone varchar(20);
 use_country varchar(20);
 use_city varchar(20);
 use_address varchar(20);
 use_genre char;
 use_dayOfBirth timestamp;
}
